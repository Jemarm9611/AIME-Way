services:
  web:
    image: node:22
    working_dir: /app
    volumes:
      - .:/app
      - base44_node_modules:/app/node_modules
    command: sh -c "npm install --no-audit --no-fund && npm run dev -- --host 0.0.0.0 --port 3000"
    environment:
      __VITE_ADDITIONAL_SERVER_ALLOWED_HOSTS: ${__VITE_ADDITIONAL_SERVER_ALLOWED_HOSTS}
    ports:
      - "3000:3000"

volumes:
  base44_node_modules:
